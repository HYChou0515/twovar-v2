# generate log
./runexp.py print
bash out_run.bash

126 log files

# generate figure

Because semigd with random is the best with r=0.5,
we commented out the r=0.1 and 0.3
./plot.py --logpath ../log/ cd

As we do it in parallel, the time is not meaningful.
Don't use it to plot time.
